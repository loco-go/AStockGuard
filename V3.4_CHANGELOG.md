# AStockGuard V3.4.0

## 目标

V3.4 把 AStockGuard 从功能堆叠式驾驶舱推进为更适合盘中执行的 Android 交易工作台。本版本仍然是决策辅助软件，不执行真实账户自动委托，也不保证交易收益。

## APK / GitHub Releases

- `master` 与任意 `feature/**` push 都会执行 test、lint、assembleDebug。
- 功能分支最新成功构建发布为 GitHub prerelease `latest-dev`。
- master 最新成功构建发布为 GitHub release `latest-master`。
- 同一通道采用滚动 Release，下一次成功构建替换前一版，避免 Releases 被开发提交刷屏。
- 当前 APK 为开发/验证签名包。正式可持续覆盖升级的 production APK 仍需仓库配置固定 release keystore secrets。

## ECharts 交易图

- 使用 Apache ECharts 6.1.0 WebView 图表层。
- 分时图：价格、均价、成交量、缩放、移动指针。
- 日K：蜡烛图、成交量、缩放。
- `TradeRecord` 中的实际 BUY/SELL 成交会映射到图上的真实成交标记。
- T 交易计划使用半透明买入区、卖出区、失效位显示，与真实成交点严格区分。
- 点击图表价格可以设定“计划买点锚点”，Android 重新计算对应卖出区和失效位；点击不会提交真实委托。

## T 交易计划

`TTradePlanner` 使用近 60 分钟价格、振幅、VWAP、可选资金流、市场阶段和现有持仓生成计划。

基础纪律：

- 缓存/过期行情：禁止实时 T 动作。
- 无至少 100 股现有底仓：不生成盘中 T 计划。
- 近 60 分钟振幅低于约 0.9%：默认 `NO_T`，避免为了交易而交易。
- T 仓默认不超过当前底仓约 1/3，并按 100 股手数取整。
- M0/M1 防守阶段把建议 T 仓压缩到最多 100 股。
- 计划包括买入区、卖出区、失效位、计划价差和理由。

A 股新买股票受 T+1 约束。本功能针对已有可卖底仓的“做 T”执行辅助；后续还需同步券商的“可用数量”才能把可卖股数约束做得完全准确。

## T 区间提醒

前台盯盘服务新增低频 T 计划扫描：

- 仅工作日 09:30–11:30、13:00–15:00。
- 仅实时行情。
- 仅扫描已有至少 100 股底仓的股票。
- 每 30 秒最多检查 6 个持仓。
- 只有状态首次进入 `BUY_ZONE`、`SELL_ZONE` 或 `INVALIDATED` 才通知，状态不变不重复推送。
- 提醒是执行辅助，不自动下单。

## 同花顺成交同步

### 只读 Accessibility

- 当前支持包名：`com.hexin.plat.android`、`com.hexin.plat.android.supremacy`。
- 用户必须自己打开同花顺当日成交/历史成交/交割单页面。
- Accessibility 只读取可访问节点中的代码、买卖方向、成交价、数量、时间。
- 不调用 `performAction()`，不自动点击、不自动买卖。
- 不保存辅助功能页面的原始文本。
- 归一化后以 `THS_ACCESSIBILITY` 写入本地 `TradeRecord`。

### 交割单文件

- 支持 CSV、TSV、TXT。
- 支持常见中英文表头别名：证券代码、买卖方向、成交价格、成交数量、日期、时间等。
- 归一化后以 `THS_FILE` 写入 `TradeRecord`。
- 文件导入作为较准确的账本路径，Accessibility 用于盘中快速补充。
- 当前没有引入 XLS/XLSX 依赖；若实际券商只提供该格式，再按真实样本适配。

两条同步路径都会按代码、方向、数量、价格、时间窗口去重，最终共用图表与复盘系统。

## UI 重构

首页从单一超长 `LazyColumn` 改为四个工作区：

1. **决策**：市场/持仓摘要、T计划、股票策略、R2、信号、新闻风险、AI判断。
2. **图表**：ECharts 分时/K线、真实成交点、T计划区、Level2。
3. **资金**：个股资金流、行业/概念资金排行、组合净值。
4. **复盘**：实际交易复盘、模拟盘、Replay、AI复盘。

新增金融终端风格 Design System：中性深浅色表面、蓝色主操作色、圆角卡片、关键数字前置，同时继续保留系统深色/浅色模式。

## 已知限制 / 下一步

- ECharts 当前通过 HTTPS CDN 加载，首次完全离线时可能无法加载；后续可改为内置静态资源。
- Accessibility 页面结构由同花顺控制，真实设备需要用实际页面验证字段顺序；无法稳定读取时以交割单文件为准。
- T 计划尚未纳入真实券商“可卖数量”、佣金最低收费、印花税与滑点后的净收益门槛。
- T 计划的真正价值必须通过后续 `TTradeEvent` 统计与“做T vs 不动”的反事实对比验证，不能用提醒次数代表收益。
