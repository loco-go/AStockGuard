# AStockGuard AI Trader

开发路线与交付计划，更新于 2026-09-08。

## 0. 项目背景

Android 单机优先的 A 股交易研究、持仓管理、盘中执行辅助与复盘工作台，围绕“发现机会 → 制定计划 → 跟踪信号 → 人工执行 → 同步成交 → 复盘验证”形成闭环。

本地确定性规则负责基础信号与风险边界，AI 负责证据整理、情景分析和计划解释。行情监控不依赖 AI 在线；真实交易由用户执行，不通过辅助功能自动提交委托。“交易合伙人”“基金经理”“资产管理”是能力阶段名称，不表示代客交易或收益承诺。

优先服务已有持仓的个人投资者：先做到数据可信、提醒可追溯、成本可计算、执行可复盘，再扩展选股、组合管理和研究。Android 正式版完成前，后端与多设备同步保持冻结。

## 1. 当前项目状态

盘点基于本地 `feature/ths-live-layout` 分支，包含截至 `07c71a3` 的功能及下方本轮交付。APK 配置为 `4.0.0 / versionCode 40`，Room 当前版本为 17。“已有实现”不代表全部通过生产验收；实际验证范围见进度记录。

| 领域 | 已有实现 | 待完善或验证 |
| --- | --- | --- |
| UI | XML 页面、ViewModel、同花顺风格详情页 | 真机交互、空态、生命周期和性能 |
| 行情 | 腾讯行情/日线/分钟数据、资金流、Room 缓存与新鲜度判断 | 交易日历、多源时间一致性、容灾 |
| 盘口 | 五档压力分析、Level2 Provider、iFinD 解析和校验测试 | 真实权限下十档完整性、缺失降级 |
| 量价与 T | 雷达、盘中信号、动态 T、可卖数量限制和成本门槛 | 成交/回补闭环、统一成本设置 |
| 成交复盘 | 同花顺只读同步、文件导入、账本、提醒效果评价 | 部分成交、重复导入、跨日对账 |
| Replay | 模拟盘、历史回放、四组量能策略对照 | 成交约束、成本一致性、样本外验证 |
| AI | 隐藏 Web 会话、API/备用 Provider、结构化解析 | 契约、恢复、证据关联和冲突验收 |
| 工程 | Room migrations、备份恢复、CI/滚动 APK 发布配置 | 升级实测、正式签名和发布验收 |

已确认的缺口：图表构建器仍通过 CDN 加载 KLineCharts，首次离线加载需处理；模拟盘存在零费用默认实现，而动态 T 已计佣金、税费与滑点，需统一口径。旧文档中的待办应结合代码核对，不重复开发已有引擎。

### 2026-09-08 开发进度

| 任务 | 状态 | 已交付与证据 | 剩余范围 |
| --- | --- | --- | --- |
| 同花顺股票与账户导入 | 已交付 | `b3001e1`、`b7cfcbc`、`f37a42f`：股票候选勾选、已有持仓选中、四项账户数据核对导入；兼容浮动盈亏和当日参考盈亏，修复金额单位及首页监控池统计 | 更多券商页面版本与异常样本持续回归 |
| 消息中心与独立通知（V3-03 子项） | 已交付 | [07c71a3](https://github.com/loco-go/AStockGuard/commit/07c71a3)：做 T、量能、行业资金独立渠道；Room 历史、分类、详情、分页和推送状态；手机两项 instrumentation tests 通过 | 完整交易日历、长时断网恢复仍未验收；行业排行不代表个股所属板块 |
| V2-01 详情页异步状态隔离 | 已完成本项回归 | 切股取消旧任务并清空日线/资金流；证券与分钟请求独立版本校验；历史日期选择阻断首次加载/实时刷新覆盖；抽取 Repository 数据边界以注入延迟样本 | V2-04 的完整交互与长时验收另行执行 |

本轮不修改 Room schema、RiskEngine 或 R2Engine，也不变更策略版本。回归样本仅用于测试，不写入用户持仓或成交。

本轮验收记录：

- `test lint assembleDebug assembleDebugAndroidTest`：通过，包含 Debug/Release 单元测试、Lint 和开发/测试 APK 构建。
- 设备：2203121C，Android 15；开发 APK 与测试 APK 覆盖安装成功。
- `StockDetailRequestTest`：2 项通过。覆盖 A→B→A 延迟返回、切股立即清空、初次加载期间选择历史日期、刷新期间选择历史日期；底层请求刻意忽略取消，验证结果写入边界。
- 当前验收是设备上的 ViewModel 并发回归，不等同于真实网络下全页面交互验收；字体/主题、旋转重建、长时盯盘与首次离线图表仍属于 V2-02/03/04。

接续顺序：V2-02 图表资源离线化及许可证 → V2-03/04 真机页面验收 → V3 成交与回补关联。V3-03 仅完成消息与部分通知验证，不能将交易日历、断网恢复或成交闭环整体标为完成。

## 2. 总体演进路线

本文 V2–V8 是产品能力阶段，与 APK 版本号独立，不表示回退版本或这些阶段全部尚未实现。发布时另行维护 APK 版本。

| 阶段 | 主题 | 核心交付 | 依赖 |
| --- | --- | --- | --- |
| V2 | 股票详情页系统 | 数据可信、图表和交互稳定 | 现有行情/UI |
| V3 | AI 交易驾驶舱 | 计划、提醒、成交、回补、复盘闭环 | V2 验收 |
| V4 | AI 机会发现系统 | 可解释、可追踪候选池 | V3 信号与验证基础 |
| V5 | AI 交易合伙人 | 同一证据下的情景建议与分歧解释 | 候选质量与 AI 契约 |
| V6 | AI 个人基金经理 | 账户风险预算、组合计划、收益归因 | 可信成交与持仓账本 |
| V7 | AI 量化研究平台 | 可复现实验、样本外评价、策略晋级 | 数据/成本版本统一 |
| V8 | AI 资产管理系统 | 多资产研究与配置辅助 | Android 正式版、V6/V7 |

近期顺序：V2 收尾 → V3 执行闭环与必要回测基础 → V4 扫描 → V5/V6 → V7 扩展 → V8。基础组合风控随 V3 落地，不等待 V6；成本和无未来数据约束也不等待 V7。

## V2 股票详情页系统

### 目标与当前问题

统一展示行情、图表、盘口、量价证据和交易计划。当前布局已提交，重点验证快速切股、刷新和后台恢复的数据一致性，解决离线图表和不同屏幕适配。

### 功能需求与页面设计

- 顶部：名称/代码、现价、涨跌幅、交易状态、来源和数据时间。
- 主区：分时、日/周/月 K 线、成交量和指标；支持缩放、十字光标及周期切换。
- 图层：实际成交、计划区间、失效位和雷达信号使用独立图例，点击查看原因与时间。
- 盘口：五档与真实十档明确区分，展示模拟、缓存、过期和缺失状态。
- 下方：量价解释、Local/AI 动作、持仓/可卖数量、动态 T 和最近提醒。
- 补全加载、空数据、无权限、重试、字体缩放和深浅色；涨跌不能只靠颜色表达。

### 数据结构与核心类

复用 `StockDetailViewModel`、`StockDetailFragment`、`ChartRepository`、`KLineChartView`、`KLineHtmlBuilder` 和 `ChartDataMapper`。页面状态包含证券、周期、来源、行情时间、新鲜度、加载状态和图层，由 ViewModel 聚合；新增类型按实际缺口确定。

### Codex 任务与验收

- [x] V2-01：核查切股/刷新链路，阻止旧请求覆盖新证券状态（2026-09-08；范围与验证见开发进度）。
- [ ] V2-02：内置图表资源并保留许可证、依赖版本，验证首次离线显示缓存图表。
- [ ] V2-03：完善图例、盘口降级、布局与无障碍显示。
- [ ] V2-04：真机覆盖快速切股、周期切换、重建、后台恢复、断网重连和长时盯盘。

完成标准：证券与证据一致；缓存不触发实时动作；图表交互不提交委托；验收记录包含设备、系统与实际结果。

## V3 AI 交易驾驶舱

### 目标

让用户清楚当前关注事项、触发原因、执行条件和失效条件，把已有量价雷达接入真实成交复盘。

### 量能分析与信号识别

复用 `IntradayVolumeAnalyzer`、`VolumeFeatureExtractor`、`VolumeSignalClassifier`、`IntradaySignalEngine` 和 `RadarNotificationPolicy`。统一量比、成交额变化、VWAP 偏离、价量背离、板块同步和盘口证据的窗口与单位。

信号保存证券、发生时间、观测价格、输入摘要、数据质量、策略版本和失效条件。缺失增强数据显式标记，不填零冒充观测。通知处理交易日历、冷却、去重、进程重启、持久化失败与过期数据阻断。

### 动态 T

复用 `DynamicTPlanner`、`StrategySignalRepository`、`ReviewRepository` 和 `AlertHistoryRepository`，完善以下闭环：

1. 保存计划快照：动作、数量、区间、失效条件、净收益估算和风险上下文。
2. 由文件、只读同步或用户记录确认真实成交；提醒不视为成交。
3. 关联计划与成交，支持部分成交、多笔成交、未执行、记录更正和重复导入。
4. 确认卖出后才建立回补上下文，依据剩余数量、资金、风险和跨日状态重新评估。
5. 分开统计计划效果、提醒效果和实际 T 净贡献，披露未关联成交与估算费用。

统一计划、模拟盘、Replay 与复盘的成本配置；实际成交费用优先于估算。可卖数量未知时不推测可卖数量；默认费率不能冒充用户券商实际费用。

### Replay

扩展已有 `VolumeStrategyReplayEngine` 的不操作、固定区间 T、动态量能 T、风险控制动态 T 四组对照。采用相同行情、初始资金/持仓与费用，展示净贡献、回撤、交易次数、卖飞和错误回补损失。历史风险或盘口缺失时披露覆盖率和对照限制，不能以默认上下文宣称真实历史表现。

### Codex 任务与验收

- [ ] V3-01：计划—提醒—成交—回补关联与状态恢复；先核查实体，再决定 schema 扩展。
- [ ] V3-02：统一成本配置和可卖数量更新，覆盖部分成交、未知数量与跨日。
- [ ] V3-03：通知去重、交易日历、新鲜度和断网恢复验证。
- [ ] V3-04：驾驶舱连接盘前计划、盘中变化、盘后复盘。
- [ ] V3-05：四组对照报告、覆盖率说明及账本指标核对。

完成标准：脱敏成交样本可复现闭环；重复导入不重复计账；无真实卖出不进入已卖回补状态；AI 不可用时本地监控继续工作。

## V4 AI 机会发现系统

### 市场扫描

先自选/持仓/指定股票池，再全市场。Repository 管理证券列表、交易状态、分批请求、限流、缓存和增量刷新。候选保存扫描时间、覆盖率、筛选/排除原因；部分请求失败不能显示为全市场无机会。

### 龙头阴 2 模型

以 `R2Engine`、`R2Scanner` 为基线，形成“龙头”“阴线”“第 2 根”“量能收缩”和失效条件的版本化可计算定义。先建立样本集，再扩展板块地位与相对强度。模型解释不擅自改写现有 R2 输出，策略变更提供新旧对照。

### 隔夜套利

作为研究模块输出收盘前观察条件、隔夜事件风险、次日退出条件和仓位约束。先历史实验及模拟验证，纳入跳空、费用和次日无法成交情景。未通过验证仅展示研究标签，“套利”名称不意味着无风险。

### 股票评分

分开显示趋势、相对强度、量价、资金、板块和风险子分，记录权重版本。资金流加速度与背离先统一单位、窗口及提供方口径。缺失字段降低覆盖率或使结果不可用；高分不越过账户风险锁定。

### Codex 任务与验收

- [ ] V4-01：候选池、扫描进度、失败重试和覆盖率展示。
- [ ] V4-02：相对强度、板块排名、资金因子和可解释评分。
- [ ] V4-03：R2 扩展与隔夜模型的版本化研究对照。
- [ ] V4-04：候选生命周期、入选快照和后续结果跟踪。

完成标准：历史输入可重现排名、无未来数据、缺失与排除可区分；满足实测数据源限额和设备预算后才开放全市场。

## V5 AI 交易合伙人

### 决策引擎

将市场风险、候选、持仓、计划和新闻汇总为有时间戳的证据包。AI 提供建议、情景、理由、触发/失效条件和不确定性；确定性风控保留阻断权限。Local Action、AI Action 与人工选择独立保存并展示。

### 多 AI 投票

单 Provider 稳定后引入多个分析角色，读取同一证据，记录 Provider/模型、提示模板版本、耗时和费用。超时或契约错误计为缺席；展示有效参与数、分歧与依据。投票一致不等于可信概率，权重调整需依据历史评价。

### 情绪周期

以市场宽度、涨跌停统计、连板表现、成交额和板块持续性建立可计算状态，保留定义版本；数据缺失输出未知。作为风险和仓位上下文，不让 AI 自由编造周期标签。

### Codex 任务与验收

- [ ] V5-01：证据包和 `<ASTOCK_STRATEGY>` 契约校验，覆盖缺字段、非法枚举、超界值和失效时间。
- [ ] V5-02：取消、超时、重试、备用 Provider 与人工恢复；原文和解析结果共同持久化。
- [ ] V5-03：分歧页面与历史建议评价，再决定是否启用加权投票。
- [ ] V5-04：情绪指标及盘前/盘中/盘后情景计划。

完成标准：AI 错误不阻塞行情；过期建议不升级为实时动作；结论可追溯证据快照。新闻和网页按不可信输入处理，不得改变系统规则或触发额外操作。

## V6 AI 个人基金经理

### 仓位管理

建立账户风险预算，区分核心仓、交易仓、可卖数量、现金与待回补数量。目标仓位转换为有成本和约束的调整计划；记录风险偏好、人工修改与计划版本。

### 组合风险

覆盖单股/行业集中度、相关性、波动、回撤、流动性和情景压力测试。相关性注明窗口、样本量和缺失情况；账户风险优先于局部高分。利润保护明确触发、恢复和失效条件，避免频繁切换。

### 资金曲线

区分入出金、交易盈亏、分红和费用，不能把追加资金算收益。展示净值、基准、回撤、仓位变化，归因拆分持有贡献、T 贡献、成本和未解释差额。行为复盘基于真实记录分析追涨、过早卖出与偏离计划，披露样本量。

### Codex 任务与验收

- [ ] V6-01：账户现金流与持仓账本对账，明确缺失初始成本处理。
- [ ] V6-02：组合风险预算、仓位建议、利润保护和压力测试。
- [ ] V6-03：净值、收益归因、行为复盘与报告导出。

完成标准：账本可与脱敏交割样本核对；入出金不制造收益；风险超限阻断冲突建议；数据不足不输出确定归因。

## V7 AI 量化研究平台

### 因子系统

统一名称、单位、回看窗口、可用时间、缺失语义和版本。保存实验数据快照、配置与结果，明确复权、停牌、退市和历史证券池处理；保留失败实验，避免只展示成功案例。

### 回测系统

扩展多日/多股组合回测、Walk Forward 和样本外留存集。实现可配置交易约束、T+1、涨跌停不可成交、滑点与成本；明确决策与成交时序，避免同根 K 线内不可知的成交假设。

报告参数敏感性、分市场阶段表现、基准比较、样本量和稳定性。排行榜使用固定评估协议；数据或费用假设变化后，旧结果标记失效或重算。

### 策略生成

AI 生成受约束的策略配置和研究假设，不直接运行任意生成代码。候选经过配置校验、回测、样本外验证和模拟观察后，才能由用户启用。Signal Fusion 保留子信号解释和冲突；动态权重仅使用决策时已获得的评价数据，并支持回退固定版本。

### Codex 任务与验收

- [ ] V7-01：因子注册、数据/配置版本和可重复实验。
- [ ] V7-02：组合回测、Walk Forward、成本敏感性与成交约束验证。
- [ ] V7-03：研究 → 回测 → 样本外 → 模拟观察 → 人工启用的策略晋级。
- [ ] V7-04：排行榜、信号融合与权重更新审计。

完成标准：相同数据/配置/随机种子得到相同结果；评价集不参与调参；研究与实时信号隔离；实验报告可独立复核。

## V8 AI 资产管理系统

### 投资委员会

汇总宏观、行业、策略与风险角色的意见，展示共识、分歧、证据时间和人工决策记录。沿用 V5 的契约与审计能力。

### 宏观分析

记录宏观数据发布时间、统计区间和修订版本；历史研究只使用当时可见版本。先明确所需来源、权限和更新频率，缺失数据不由 AI 补造。

### 资产配置

先支持用户录入/导入资产汇总，再按数据许可扩展资产类型。统一币种和估值时间，考虑期限、流动性、集中度及再平衡成本，输出情景与人工调整计划。

### Codex 任务与准入

- [ ] V8-01：Android 正式版验收后，重新确认用户、资产范围与数据预算。
- [ ] V8-02：多资产账户、估值/币种和配置情景比较。
- [ ] V8-03：投资议案、人工选择与再平衡复盘。
- [ ] V8-04：确认多设备需求后，另行设计后端身份、加密、同步冲突、导出和删除。

完成标准：资产估值口径明确；缺失价格不当作零资产；建议可解释且真实操作由用户执行。V8 为远期规划，不表示当前数据源已覆盖所需资产。

## Codex 总开发规则

### 架构要求

- `master` 是已验证基线，禁止直接开发；使用 `feature/*`、`fix/*`、`refactor/*`，通过 PR 合并。
- UI 观察 ViewModel 状态，一次性导航/工作使用 effects/events；不直接调用腾讯、东方财富或 AI 客户端。
- Repository 决定远端/本地来源；Room 是已持久化行情与策略数据的本地事实来源。
- `RiskEngine`、`R2Engine` 保持确定性；重构保留输出，策略变更需版本化和对照。
- Web AI 为可选能力，正常隐藏运行，仅登录/验证/DOM 恢复使用可见页面。
- 优先复用现有类；规划中的数据概念不等于必须创建同名类或重复引擎。

### 数据要求

- 显式记录来源、证券、事件/抓取时间、新鲜度、完整性和策略版本；区分零值、缺失、模拟和缓存。
- 过期/缓存不能发出实时 BUY/SELL/T 提醒；多源证据参与动作前校验证券及时间一致性。
- Room schema 变化需升级版本、migration 和 `app/schemas/`；生产迁移前补迁移测试，不以清库替代迁移。
- AI 原文与 `<ASTOCK_STRATEGY>` 解析结果共同保存；本地/AI 动作独立，冲突显式展示。
- 不提交凭据、Cookie、Session、API Key；备份与诊断导出排除敏感数据。
- 实现交易规则、费率、时段和数据权限时核验适用来源与生效范围；本文不充当实时规则清单。

### 测试要求

| 改动 | 必须验证 |
| --- | --- |
| 策略/风控 | 边界、确定性、版本对照、过期阻断 |
| 数据源/同步 | 样本解析、时间/单位、缺失、重复、失败恢复 |
| Room/备份 | 升级保留数据、迁移路径、恢复兼容、敏感字段排除 |
| UI | 真机交互、主题/字体、切股、后台和离线 |
| Replay/账本 | 成本/现金流核对、成交约束、无未来数据、基准 |
| AI | 契约错误、超时、缺席、冲突、取消、本地独立运行 |

功能 PR 包含 `test`、`lint`、`assembleDebug`；设备能力/迁移增加相关 instrumentation tests。纯文档检查内容、链接和差异即可。记录实际运行范围，未运行不写成通过。

正式发布需固定签名、覆盖升级、备份恢复、关键真机流程、离线容灾和长时盯盘验收。先测目标设备基线，再为刷新延迟、内存、耗电和请求量设定任务级预算与阈值。发布附已知限制和问题恢复方案，不通过降级清库恢复数据。

### 开发顺序

| 批次 | 范围 | 建议分支 | 交付物 |
| --- | --- | --- | --- |
| 1 | V2 页面验收、状态与离线图表 | 当前布局分支收尾；后续 `fix/chart-offline` | 验收记录、内置资源、PR |
| 2 | V3 成交与回补关联 | `feature/t-trade-lifecycle` | 状态恢复、必要 migration 和测试 |
| 3 | V3 成本、可卖数量与对账 | `feature/trading-cost-model` | 共用配置、样本对账 |
| 4 | V3 通知与四组回放 | `feature/trading-validation` | 对照报告、覆盖率和通知验证 |
| 5 | V4 指定池到全市场扫描 | `feature/opportunity-scanner` | 候选池、进度、评分与跟踪 |
| 6 | V5 AI 与 V6 组合管理 | 按能力拆分 | 证据契约、风险预算、归因 |
| 7 | V7 研究与正式版验收 | 按能力拆分 | 可复现实验、策略晋级、发布包 |
| 8 | V8 需求评审 | 准入后确定 | 多资产范围与实现方案 |

每项开始先核对已有实现，记录输入、输出、依赖和验收样例；完成后附 PR、实际验证结果及限制，再勾选。批次 2/3 在设计时统一数据契约，按可独立验收的 PR 交付。完成日期依据真机结果、数据权限与任务规模安排，不预设未经评估的工期。

外部依赖包括脱敏成交/持仓样本、真机、Level2 权限、AI Provider 配置和正式签名。缺少某项时继续完成本地逻辑与样本验证，标明未完成的集成验收，不以模拟数据宣称实盘通过。
